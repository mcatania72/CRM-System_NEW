// backend/src/__tests__/sample.test.ts

describe('Sample Backend Test Suite', () => {
  it('should be true', () => {
    expect(true).toBe(true);
  });

  it('should perform basic math', () => {
    expect(2 + 2).toBe(4);
    expect(10 * 10).toBe(100);
  });
});
